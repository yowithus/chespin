import { getQuote, getColor, setModalVisible } from './quote';
import { getColors, setColor } from './color';

const actionCreators = {
  getQuote,
  getColor,
  setModalVisible,
  getColors,
  setColor
};

export default actionCreators;
